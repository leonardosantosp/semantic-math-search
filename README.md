# semantic-math-search
A semantic math-search engine that bridges user queries with multiple Elasticsearch indexes. It supports two retrieval modes: direct formula-embedding search and tokenized-formula embedding search. The project handles embedding generation, query processing, and KNN lookup across both pipelines.
